import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { useAuth } from '../../context/AuthContext'
import { AuthShell } from '../../components/layout/PageLayouts'
export default function LoginPage() { const [formData, setFormData] = useState({ email: '', password: '' }); const { login } = useAuth(); const navigate = useNavigate(); const handleSubmit = (event) => { event.preventDefault(); login({ email: formData.email, name: formData.email.split('@')[0] }); navigate('/account') }; return <AuthShell eyebrow="Welcome back" title="Sign in." intro="Pick up right where you left off." footer={<>New to FLEX? <Link to="/register">Create an account</Link></>}><form className="auth-form" onSubmit={handleSubmit}><label>Email<input type="email" placeholder="you@example.com" value={formData.email} onChange={(event) => setFormData({ ...formData, email: event.target.value })} required /></label><label>Password<input type="password" placeholder="Your password" value={formData.password} onChange={(event) => setFormData({ ...formData, password: event.target.value })} required /></label><Link className="auth-forgot" to="/forgot-password">Forgot your password?</Link><button type="submit">Sign in <span>→</span></button></form></AuthShell> }
